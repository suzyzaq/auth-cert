# 备份与恢复

## 备份

1. RDS 开启每日全量和日志备份，保留不少于 30 天。
2. 每次回写前记录源版本，并生成受影响记录的只读快照。
3. OSS 查询分片启用版本控制，暂存目录至少保留 7 天。
4. 每季度在隔离环境执行一次恢复演练并记录耗时。

## 恢复顺序

先暂停巡检任务和回写，再恢复数据库到指定时间点；核对记录数与抽样字段后，
从已验证的 OSS 版本恢复查询分片。恢复完成前不得重新开启回写。

